# Copy this file to `proxy.env` if you only want gateway/opencode
# to use a proxy while keeping other apps on direct network.
#
# Example for Clash/V2ray local HTTP proxy port 7890:
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
ALL_PROXY=socks5://127.0.0.1:7891

# Keep local calls direct, important for APK -> gateway localhost calls.
NO_PROXY=127.0.0.1,localhost
